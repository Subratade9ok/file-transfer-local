<%- include('partials/header') %>

  <h2 class="font-bold text-lg mb-2 bg-linear-to-r from-indigo-500 to-cyan-500 text-transparent bg-clip-text">
    Enter PIN
  </h2>

  <form method="post" class="md:w-64 bg-white p-4 rounded shadow">
    <div class="relative p-0.5 mb-2 bg-linear-to-r from-indigo-500 to-cyan-500 rounded">
      <input type="password" name="pin" placeholder="PIN" class="w-full p-2 border-0 outline-0  bg-white" autofocus>
    </div>
    <div class="w-full flex flex-row flex-nowrap gap-x-2">
      <button type="reset" onclick="window.history.back()" class="flex-2 bg-linear-to-r from-red-800 to-pink-700 text-white w-full py-1 rounded cursor-pointer">
        Cancel
      </button>
      <button class="flex-4 bg-linear-to-r from-indigo-500 to-cyan-500 text-white w-full py-1 rounded cursor-pointer">
        🔓 Unlock
      </button>
    </div>
  </form>

  <% if (typeof error !=="undefined" && error) { %>
    <p class="text-red-600 mt-2">
      <%= error %>
    </p>
    <% } %>

      <%- include('partials/footer') %>