<%- include('partials/header') %>

<h1 class="font-bold text-lg mb-3 bg-gradient-to-r from-indigo-500 to-cyan-500 text-transparent bg-clip-text">
  Browsing: /<%= current || '' %>
</h1>

<p class="mt-4 py-3 text-xs flex justify-between">
  <a href="/" class="underline text-indigo-600">&lt; Upload</a>
  <a href="/logout" class="text-xs underline text-red-600">Logout</a>
</p>

<ul class="bg-white rounded shadow p-3">
  <% items.forEach(item => { %>
    <li class="mb-1 flex items-center gap-2">
      <% if (item.isDir) { %>
        📁
        <a
          href="/browse?path=<%= item.path %>"
          class="text-blue-600 underline"
        >
          <%= item.name %>
        </a>
      <% } else { %>
        📄
        <a
          href="/browse/download?file=<%= item.path %>"
          class="underline"
        >
          <%= item.name %>
        </a>
      <% } %>
    </li>
  <% }) %>
</ul>

<%- include('partials/footer') %>
